export const CONFIG = {
// i don't even think this is used either
    fridgeWidth: 800,
    fridgeHeight: 600,
// we don't have this implemented yet lol ;-;
    snapToGrid: false,
    gridSize: 20
};
